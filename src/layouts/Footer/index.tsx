import * as React from "react";

interface IFooterProps {}

const Footer: React.FunctionComponent<IFooterProps> = (props) => {
  return <div className="w-full h-8 bg-purple-500">Footer</div>;
};

export default Footer;
