import * as React from "react";

interface ISidebarProps {}

const Sidebar: React.FunctionComponent<ISidebarProps> = (props) => {
  return <div className="w-full h-8 bg-gray-500">Sidebar</div>;
};

export default Sidebar;
